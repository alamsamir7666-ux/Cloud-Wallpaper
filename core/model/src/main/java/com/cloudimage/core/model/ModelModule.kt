package com.cloudimage.core.model

/**
 * :core:model — app-internal domain models.
 *
 * Part 2 adds repositories and mappings for favorites, history and
 * download records. The public provider contract lives in :provider:api
 * so extension authors only need that one artifact.
 */
object ModelModule
